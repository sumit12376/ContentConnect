<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * {
            margin: 0;
            padding: 0;
          
        }
    </style>
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Home - My Blog</title>
</head>

<body class="bg-gray-900 text-white">
    <div class="w-full min-h-screen">

        <nav class="bg-gray-800 shadow-lg rounded-lg mb-0 p-0 flex justify-between items-center">
            <div class="text-lg mb-10 flex ml-4  font-bold  text-blue-400 mt-5">
                <a href="/home" class="hover:text-blue-300 transition duration-300">Home</a>
                <% if (!currentUser) { %>
                    <a href="/login" class="ml-4 hover:text-blue-300 transition duration-300">Login</a>
                    <a href="/create" class="ml-4 hover:text-blue-300 transition duration-300">Create Account</a>
                <% } else { %>
                    <a href="/profile" class="ml-4 hover:text-blue-300 transition duration-300">Profile</a>
                    <a href="/allposts" class="ml-4 hover:text-blue-300 transition duration-300">All Posts</a>
                    
                <% } %>
            </div>
            <div>
                <% if (currentUser) { %>
                    <button type="button"
                        class="bg-red-600 text-white font-bold py-2 px-4 mr-4 rounded-lg hover:bg-red-700 transition duration-300"
                        onclick="window.location.href='/logout'">
                        Logout
                    </button>
                <% } %>
            </div>
        </nav>

        <div class="relative bg-cover bg-center h-screen w-full" style="background-image: url('./images/uploads/image.png');">
            <div class="flex items-center justify-center h-full bg-black bg-opacity-60">
                <div class="text-center text-white p-8">
                    <h1 class="text-4xl md:text-6xl font-bold mb-4">Welcome to Our Website</h1>
                    <p class="text-lg mb-8">Discover amazing content and resources.</p>
                    
                </div>
            </div>
        </div>
    </div>
</body>

</html>
